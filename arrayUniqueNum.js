let arrayUnique = ([1, 2, 3, 4, 1, 3, 5, 10, 16]);

let b = [];

let len = arrayUnique.length;

for(let i = 0; i < len; i++){
    if(b.indexOf(arrayUnique[i]) === -1){
        b.push(arrayUnique[i]);
    }else (arrayUnique)
}
console.log(b);
